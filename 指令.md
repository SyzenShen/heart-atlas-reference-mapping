你现在是这个项目的首席单细胞计算生物学工程师。请在当前工作目录中创建并尽可能执行一个完整、可复现、适合公开到 GitHub 的 mini-project。

项目名称：

heart-atlas-reference-mapping

项目标题：

Donor-held-out reference mapping of the Human Heart Cell Atlas with scVI, scANVI, and scArches

项目目标：

1. 使用 scvi-tools 提供的20,000-cell subsampled Human Heart Cell Atlas。
2. 将一个完整供体作为从未见过的 query donor，其余供体作为 reference。
3. 在reference上训练scVI和scANVI。
4. 使用scArches query mapping将query donor映射到reference latent space。
5. 在训练和映射过程中隐藏query donor的真实cell-type labels。
6. 完成后才使用query donor的真实labels进行评价。
7. 与一个简单的PCA + kNN baseline比较。
8. 分析scANVI的prediction confidence能否识别容易出错的细胞。
9. 最终形成一个适合公开展示的GitHub项目，但不要声称复现了整篇论文或发现了新的生物学结论。

核心科学问题：

Can scANVI/scArches accurately transfer cardiac cell-type labels to a donor that was completely unseen during reference training, and can prediction confidence identify unreliable cross-donor annotations?

====================
一、必须阅读和核对的官方资料
==============

开始编码前，必须检查以下资料当前版本和API，不要仅凭记忆编写scvi-tools代码：

1. scArches论文

Mapping single-cell data to reference atlases by transfer learning

https://doi.org/10.1038/s41587-021-01001-7

2. Theis Lab scArches repository

https://github.com/theislab/scarches

3. scvi-tools官方reference-mapping tutorial

https://docs.scvi-tools.org/en/stable/tutorials/notebooks/scrna/scarches_scvi_tools.html

如果stable页面路径发生变化，请从当前scvi-tools文档中搜索“Reference mapping with scvi-tools”。

4. scANVI官方API

https://docs.scvi-tools.org/en/stable/api/reference/scvi.model.SCANVI.html

5. scVI官方API

https://docs.scvi-tools.org/en/stable/api/reference/scvi.model.SCVI.html

6. 内置心脏图谱数据

https://docs.scvi-tools.org/en/stable/api/reference/scvi.data.heart_cell_atlas_subsampled.html

加载方式预期为：

import scvi
adata = scvi.data.heart_cell_atlas_subsampled()

但必须用当前安装版本核实。

7. Human Heart Cell Atlas

https://www.heartcellatlas.org/

8. scANVI论文

Probabilistic harmonization and annotation of single-cell transcriptomics data with deep generative models

https://doi.org/10.15252/msb.20209620

所有实际使用的API、版本和参数必须来自当前官方文档与运行时检查。不要复制网络上已经过期的旧版notebook代码。

====================
二、科研诚信和防止数据泄漏的规则
================

这些规则优先级最高。

1. 禁止按cell随机划分train/test。

必须按完整donor划分：

* reference：若干完整供体；
* query：一个完整、训练中未见过的供体。

2. query donor的真实cell-type labels只能用于最终评价。

创建query数据后：

* 将真实标签复制到只用于evaluation的独立对象或独立文件；
* 模型实际接收的query label必须全部设为scANVI所需的unlabeled category，例如“Unknown”；
* 不得使用query真实标签训练模型；
* 不得根据query最终accuracy选择超参数；
* 不得根据多个query donor的最终成绩挑一个最好看的结果作为主结果。

3. 所有可学习的预处理必须只在reference上拟合。

包括：

* highly variable gene selection；
* PCA；
* kNN classifier；
* scaling参数；
* feature selection；
* thresholds之外的任何数据驱动参数。

query只能应用reference已经确定的genes和transform。

4. scVI/scANVI必须使用raw count数据。

不得把log-normalized expression当作counts输入negative-binomial模型。

必须检查：

* `adata.X`
* `adata.layers`
* `adata.raw`
* 数据值范围
* 是否大致为非负整数
* 稀疏矩阵类型
* 官方dataset loader的说明或source code

在确认counts位于哪个位置后，才设置：

layer="counts"

如果没有可信的raw counts，停止模型训练并明确报告，不能静默使用log-normalized values。

5. 不要把UMAP当作定量证据。

UMAP仅用于可视化。主要结论必须来自：

* accuracy
* balanced accuracy
* macro-F1
* weighted-F1
* per-class precision/recall/F1
* confusion matrix
* confidence–coverage分析

6. 不得伪造结果。

如果没有GPU、安装失败、数据字段不满足要求或API发生变化：

* 仍然完成项目结构、文档、测试和Colab notebook；
* 明确标记实验为pending；
* 不生成假metrics、假图或假模型；
* 不在README中声称实验成功。

7. 不得把模型confidence直接称为“校准后的概率”。

默认称为：

prediction confidence score

只有完成明确的calibration分析后，才能讨论calibrated probability。

8. 一个query donor不是生物学重复。

即使query包含数千个cells，也只能说：

* one held-out donor；
* cell-level evaluation within that donor。

不能将cells当作独立donor replicates，也不能声称结果能泛化到所有人类供体。

====================
三、第一阶段：环境审计
===========

先进行只读环境检查，并写入：

docs/environment_audit.md

记录：

* 操作系统
* CPU
* 内存
* 磁盘空间
* Python版本
* conda/mamba是否存在
* git版本
* PyTorch版本
* PyTorch CUDA是否可用
* NVIDIA GPU型号和显存
* Apple MPS是否可用
* scvi-tools是否已安装
* scanpy、anndata、scikit-learn、pandas版本
* 当前目录是否为git仓库
* 是否存在用户未提交修改
* 是否能访问官方数据和GitHub

选择执行后端：

A. 本地NVIDIA GPU可用：

建立隔离环境并尝试执行正式实验。

B. 本地只有CPU或Apple MPS：

可以运行数据审计、测试、baseline和smoke experiment。

只有在当前scvi-tools/PyTorch Lightning明确、可靠支持MPS时才使用MPS进行正式训练；否则生成Google Colab notebook完成GPU训练。

C. 环境不完整：

完成静态项目、测试和Colab入口，不伪造结果。

不要在用户已有项目中覆盖文件。请创建独立目录：

heart-atlas-reference-mapping/

====================
四、项目结构
======

创建以下结构：

heart-atlas-reference-mapping/
├── README.md
├── LICENSE
├── CITATION.cff
├── .gitignore
├── environment.yml
├── requirements-lock.txt
├── configs/
│   ├── smoke.yaml
│   └── main.yaml
├── notebooks/
│   ├── 00_data_audit.ipynb
│   ├── 01_baseline_pca_knn.ipynb
│   ├── 02_reference_scvi_scanvi.ipynb
│   ├── 03_query_mapping.ipynb
│   └── 04_evaluation_and_figures.ipynb
├── scripts/
│   ├── check_environment.py
│   ├── audit_data.py
│   ├── prepare_split.py
│   ├── run_baseline.py
│   ├── train_reference.py
│   ├── map_query.py
│   ├── evaluate.py
│   ├── make_figures.py
│   └── verify_outputs.py
├── src/
│   └── heartmap/
│       ├── **init**.py
│       ├── config.py
│       ├── data.py
│       ├── split.py
│       ├── baseline.py
│       ├── models.py
│       ├── metrics.py
│       ├── confidence.py
│       ├── provenance.py
│       └── plotting.py
├── tests/
│   ├── test_data_validation.py
│   ├── test_donor_split.py
│   ├── test_no_label_leakage.py
│   ├── test_reference_only_preprocessing.py
│   ├── test_metrics.py
│   ├── test_confidence.py
│   └── test_result_schema.py
├── data/
│   ├── README.md
│   ├── raw/.gitkeep
│   ├── processed/.gitkeep
│   └── splits/.gitkeep
├── models/
│   └── .gitkeep
├── results/
│   ├── README.md
│   ├── metrics/.gitkeep
│   └── predictions/.gitkeep
├── figures/
│   └── .gitkeep
└── docs/
├── METHOD.md
├── RUNBOOK.md
├── COLAB_RUNBOOK.md
├── METRICS.md
├── DATA_DICTIONARY.md
├── LEAKAGE_CHECKLIST.md
├── TROUBLESHOOTING.md
├── environment_audit.md
├── provenance.md
└── scientific_notes.md

大型`.h5ad`和模型权重默认不得提交到git。

====================
五、数据审计
======

运行：

adata = scvi.data.heart_cell_atlas_subsampled()

然后生成：

docs/DATA_DICTIONARY.md
results/data_audit.json
results/obs_columns.csv

必须报告：

* n_obs
* n_vars
* `adata.X`类型、shape、dtype
* layers及每层含义
* raw是否存在
* obsm、varm、uns包含什么
* 所有`adata.obs`列名
* 每个obs列的dtype
* 每个分类列的unique value数量
* 每个可能donor列的value counts
* 每个可能batch列的value counts
* 每个可能cell-type label列的value counts
* 是否有study、sample、donor、region、assay等字段
* 缺失值数量
* nuisance clusters、doublets或unassigned cells是否已移除
* counts实际存储位置
* counts是否非负
* 对非零值抽样检查是否接近整数
* 每个donor的cell数量
* donor × cell type交叉表

不要根据字段名称盲猜。

在docs/DATA_DICTIONARY.md中明确写出最终选择：

* donor_key
* batch_key
* cell_type_key
* counts_layer
* 选择理由
* 未采用的候选字段及理由

cell_type_key必须优先使用作者提供的生物学annotation，而不是Leiden cluster编号。如果存在broad cell type和fine cell state两个层级，主实验优先使用broad cell type；fine state分析可列为future work，不要扩张当前scope。

====================
六、确定query donor
===============

query donor必须根据预先写明的确定性规则选择，而不是训练后挑最好看的donor。

推荐规则：

1. 排除缺失donor信息的cells。
2. 排除明显的doublet/unassigned/nuisance labels。
3. 为每个donor统计：

   * 总cell数；
   * broad cell-type数量；
   * 每个cell type的support。
4. eligible donor必须满足：

   * 至少500个cells；如果没有donor达到，合理降低阈值并记录；
   * 至少5个broad cell types；
   * 主要cell types在剩余reference donors中也存在。
5. 从eligible donors中选择cell数最多的一个作为query donor。
6. 如并列，按照donor ID字符串排序，选择第一个。
7. 将选择规则、候选donors和最终query donor写入：

   * results/query_selection.csv
   * docs/provenance.md

这个规则可以在选择阶段查看metadata labels以保证benchmark可评价，但不得根据任何模型性能更换query donor。

划分完成后保存：

data/splits/split_manifest.json

其中记录：

* query donor ID
* reference donor IDs
* reference cell IDs的SHA256或稳定hash
* query cell IDs的SHA256或稳定hash
* donor_key
* cell_type_key
* seed
* selection rule
* scvi-tools版本
* dataset fingerprint

不要提交包含敏感个人信息的metadata。该公开atlas通常是去标识化数据，但仍应只保留分析必要字段。

====================
七、query label sealing
=====================

必须实现明确的label sealing机制。

创建：

1. `query_eval_labels.csv`

仅含：

* cell_id
* true_cell_type

该文件只能由最终evaluation脚本读取。

2. 用于模型的query AnnData：

* 真实cell-type列删除或移出；
* 模型labels_key全部设置为统一的unlabeled category，例如`Unknown`；
* 不得在模型对象或训练数据中保留另一个容易误用的真实标签副本。

实现自动测试：

* 训练前检查query model input中不存在真实label列；
* 所有query labels均为Unknown；
* evaluation代码不能被训练脚本import；
* reference和query cell IDs无交集；
* reference和query donor IDs无交集。

如果任一检查失败，训练必须停止。

====================
八、feature selection与预处理
=======================

主模型：

1. 只使用reference选择highly variable genes。
2. 默认选择2,000 HVGs；最终参数写入YAML。
3. batch_key使用审计后确认的字段。
4. query按照reference确定的gene set进行subset/reorder。
5. 使用官方`prepare_query_anndata()`处理基因顺序或缺失基因。
6. 不在reference和query合并数据上重新选择HVG。
7. 不对scVI输入做log1p替代raw counts。
8. 所有预处理决策记录在metadata.json。

baseline：

1. 使用同一套reference-derived HVGs。
2. 分别对reference和query进行library-size normalization和log1p。
3. PCA只能在reference上fit。
4. query只能调用PCA transform。
5. kNN classifier只能在reference latent/PCA坐标和reference labels上fit。
6. query真实labels仅在预测完成后用于评价。

如果由于Scanpy/PCA稀疏矩阵限制需要额外转换，必须记录并避免不必要的整矩阵densification。

====================
九、配置文件
======

所有参数必须来自YAML。

smoke.yaml：

* 使用少量reference和query cells；
* 只验证代码路径；
* epochs很少；
* 结果必须标为smoke，不可用于科学结论；
* 可以使用synthetic AnnData进行单元测试；
* synthetic结果不得进入正式results。

main.yaml至少包含：

experiment_name: heart_donor_holdout
seed: 42
dataset: heart_cell_atlas_subsampled
n_hvg: 2000
query_selection_rule: largest_eligible_donor
minimum_query_cells: 500
minimum_query_cell_types: 5
unlabeled_category: Unknown

baseline:
method: pca_knn
n_components: 30
n_neighbors: 15
weights: distance

scvi:
n_latent: 30
n_layers: 2
dropout_rate: 0.2
use_layer_norm: both
use_batch_norm: none
encode_covariates: true
max_epochs: auto_or_documented
early_stopping: true

scanvi:
max_epochs: documented_value
n_samples_per_label: documented_value
early_stopping: true

query_mapping:
max_epochs: documented_value
weight_decay: 0.0
early_stopping: true

confidence_thresholds:

* 0.0
* 0.5
* 0.6
* 0.7
* 0.8
* 0.9

不要机械使用这里的训练epoch。先查当前官方tutorial和dataset规模，然后选合理值并在METHOD.md解释。主实验参数一旦确定并开始正式评价，不得根据query成绩调参。

====================
十、PCA + kNN baseline
====================

baseline的目的不是追求最强性能，而是回答深度生成模型是否比简单方法提供额外价值。

流程：

reference raw counts
→ reference-derived HVGs
→ normalize_total
→ log1p
→ PCA fit on reference
→ kNN fit on reference PCA + labels
→ transform query
→ predict query labels
→ predict_proba或邻居投票confidence
→ 最终evaluation

保存：

results/predictions/baseline_predictions.csv

至少包含：

* cell_id
* predicted_label
* confidence
* query_donor
* method

真实label不要由baseline脚本写入预测文件。evaluation阶段再根据cell_id与sealed labels合并。

记录：

* PCA explained variance
* k
* distance metric
* weights
* fit time
* prediction time
* reference cell count
* query cell count

====================
十一、scVI reference model
=======================

根据当前官方API实现，概念流程应为：

1. 在reference AnnData上调用`SCVI.setup_anndata()`。
2. layer指向确认的raw counts。
3. batch_key使用确认后的reference batch/donor字段。
4. 创建SCVI model。
5. 使用适合scArches的参数：

   * layer norm
   * no batch norm
   * encode covariates
   * dropout
   * documented architecture
6. 训练。
7. 保存模型。
8. 保存reference latent representation。
9. 保存training history。
10. 记录随机种子、设备、运行时间和软件版本。

输出：

models/scvi_reference/
results/reference_latent.h5ad
results/training/scvi_history.csv
results/training/scvi_summary.json

如果当前API与旧tutorial不同，以当前stable API为准，并在provenance中记录变化。

====================
十二、scANVI reference model
=========================

概念流程：

1. 从已训练的SCVI model初始化SCANVI。
2. labels_key只使用reference真实labels。
3. 指定unlabeled_category。
4. 训练scANVI。
5. 保存模型。
6. 提取reference的scANVI latent representation。
7. 保存training history。

输出：

models/scanvi_reference/
results/reference_scanvi_latent.h5ad
results/training/scanvi_history.csv
results/training/scanvi_summary.json

训练前再次验证：

* reference没有Unknown以外的异常label；
* query data没有参与reference model training；
* query donor ID未出现在reference；
* feature set只由reference确定。

====================
十三、scArches query mapping
=========================

根据当前scvi-tools官方API执行：

1. 使用`prepare_query_anndata()`或当前等价API；
2. 验证query genes与reference model兼容；
3. 使用`load_query_data()`或当前等价API加载reference model；
4. query labels全部为Unknown；
5. query mapping训练时使用官方推荐的`weight_decay=0.0`；
6. 不解冻不应更新的reference参数；
7. 记录实际可训练参数数量；
8. 映射后得到query latent representation；
9. 使用scANVI预测query labels；
10. 使用soft prediction输出每一类的score；
11. confidence定义为最大class score；
12. 额外保存prediction entropy。

输出：

models/scanvi_query/
results/query_mapped.h5ad
results/predictions/scanvi_predictions.csv
results/training/query_mapping_history.csv
results/training/query_mapping_summary.json

predictions CSV至少包含：

* cell_id
* predicted_label
* confidence
* entropy
* query_donor
* method
* status

可以另外保存每个class的score矩阵，但注意文件大小。

不得在这一步读取query_eval_labels.csv。

====================
十四、联合可视化
========

构建reference + query的联合latent representation。

要求：

* 使用模型产生的latent representation；
* UMAP neighbors基于`X_scANVI`；
* 固定UMAP seed；
* 不在原始gene-expression matrix上直接计算联合UMAP并称为模型结果。

保存：

figures/umap_raw_by_donor.png
figures/umap_scanvi_by_donor.png
figures/umap_scanvi_by_cell_type.png
figures/umap_scanvi_query_predictions.png

`umap_scanvi_by_cell_type.png`中：

* reference使用真实cell types；
* query只能在评价图中显示真实cell types；
* 预测图与真实图必须明确区分；
* 不得让读者误以为query真实labels参与训练。

UMAP只能作为辅助展示，不能作为“batch effect已消除”或“模型优于baseline”的唯一证据。

====================
十五、evaluation
=============

只有evaluation脚本可以读取：

data/splits/query_eval_labels.csv

根据cell_id把真实label与predictions合并。

对baseline和scANVI分别计算：

* n_query_cells
* accuracy
* balanced_accuracy
* macro_f1
* weighted_f1
* per-class precision
* per-class recall
* per-class f1
* per-class support
* confusion matrix
* normalized confusion matrix

保存：

results/metrics/summary.csv
results/metrics/per_class.csv
results/metrics/confusion_baseline.csv
results/metrics/confusion_scanvi.csv
results/metrics/evaluation_metadata.json

对于query中reference从未出现的cell type：

* 不得强行算作普通closed-set label；
* 单独标记为out-of-reference；
* 报告其数量；
* 分别给出：

  * all-query evaluation
  * closed-set evaluation

如果没有out-of-reference cell type，也要明确写明。

对于极少数cell types：

* 不要因为表现差就删除；
* 在per-class table中保留；
* 主macro-F1可以另外定义“evaluable classes”，但必须预先设定support threshold并同时报告全部类别结果。

====================
十六、confidence-aware analysis
============================

对于每个threshold t：

保留：

confidence >= t

计算：

* retained_cells
* coverage = retained_cells / total_query_cells
* accuracy_on_retained
* balanced_accuracy_on_retained
* macro_f1_on_retained
* error_rate_on_retained
* rejected_cells
* rejected-cell true-type composition

保存：

results/metrics/confidence_coverage.csv

作图：

figures/coverage_accuracy.png
figures/coverage_macro_f1.png
figures/confidence_correct_vs_incorrect.png
figures/confidence_by_cell_type.png

解释规则：

* 如果threshold提高、accuracy提高，只能说高confidence predictions在该held-out donor中更可靠；
* 不得声称confidence已经校准；
* 不得从一个donor推断普遍规律；
* 如果稀有细胞更多被拒绝，要明确指出潜在公平性/coverage问题；
* 如果错误预测也很高confidence，要重点讨论，而不是隐藏。

可选计算ECE或reliability diagram，但必须明确这是描述性calibration分析。不要为了增加图表而强行加入。

====================
十七、主要图表
=======

正式README最多展示以下核心图：

1. `umap_raw_by_donor.png`
2. `umap_scanvi_by_donor.png`
3. `confusion_scanvi_normalized.png`
4. `method_comparison.png`
5. `f1_vs_cell_abundance.png`
6. `coverage_accuracy.png`

图表要求：

* 清楚写轴名称；
* 写明单位；
* 写明query donor；
* 写明n cells和n donors；
* confusion matrix同时提供raw counts和row-normalized版本；
* method comparison必须包含baseline；
* F1 vs abundance中的每个点代表一个cell type；
* 不使用三维图；
* 不通过截断坐标轴夸大差异；
* 不画没有真实数据支撑的图；
* smoke结果不得进入正式figures。

====================
十八、结果解释边界
=========

README和analysis summary必须明确：

1. 这是一个held-out donor case study。
2. 不是完整scArches论文复现。
3. 不是Human Heart Cell Atlas重新构建。
4. 不是新的cell type发现。
5. query真实labels仅用于post hoc evaluation。
6. 高accuracy不等于所有稀有cell types都表现良好。
7. UMAP混合不自动等于生物学正确。
8. softmax/confidence score不一定经过校准。
9. 同一donor中的cells不是独立生物学重复。
10. 若要获得可泛化结论，需要多个held-out donors和预注册的重复实验。

推荐措辞：

This project reproduces and adapts the scArches/scANVI reference-mapping workflow to a donor-held-out Human Heart Cell Atlas case study.

禁止措辞：

We reproduced the scArches paper.

Our model generalizes to unseen humans.

The batch effect was completely removed.

The model discovered novel cardiac cell types.

====================
十九、README结构
===========

README使用清晰、克制的英文，包含：

1. Title
2. One-paragraph summary
3. Scientific question
4. Why donor-held-out evaluation matters
5. Dataset
6. scVI, scANVI, and scArches in plain language
7. Experimental design
8. Leakage prevention
9. PCA + kNN baseline
10. Reference-model training
11. Query mapping
12. Evaluation metrics
13. Confidence-aware abstention
14. Results
15. Per-cell-type failure analysis
16. Limitations
17. Repository structure
18. Installation
19. Local execution
20. Google Colab execution
21. Reproducibility and provenance
22. What I learned
23. Open questions
24. References
25. License

README应能回答：

* reference是什么？
* query是什么？
* 为什么不能随机按cell切分？
* scVI与scANVI有什么区别？
* scArches在query mapping中做了什么？
* 为什么需要baseline？
* macro-F1为什么重要？
* confidence threshold为什么会牺牲coverage？
* 模型最容易错在哪些cell types？
* 当前结果能支持什么，不能支持什么？

====================
二十、notebook要求
=============

00_data_audit.ipynb：

* 加载数据；
* 展示AnnData结构；
* 检查counts；
* 展示obs字段；
* donor和cell type分布；
* 生成donor × cell type表；
* 决定keys；
* 不训练模型。

01_baseline_pca_knn.ipynb：

* 加载固定split；
* reference-only preprocessing；
* PCA；
* kNN；
* 预测query；
* 保存结果；
* 不读取query真实labels。

02_reference_scvi_scanvi.ipynb：

* 加载固定split；
* 验证无leakage；
* 训练scVI；
* 初始化并训练scANVI；
* 保存模型和latent；
* 绘制训练曲线；
* 不加载query labels。

03_query_mapping.ipynb：

* 加载reference model；
* 准备query；
* 验证labels全部为Unknown；
* 执行scArches mapping；
* 生成prediction、confidence和entropy；
* 保存query model与latent；
* 不读取真实query labels。

04_evaluation_and_figures.ipynb：

* 读取已保存predictions；
* 读取sealed labels；
* 合并；
* 计算baseline和scANVI指标；
* confusion matrix；
* per-class analysis；
* confidence–coverage；
* 生成正式图；
* 自动生成results/analysis_summary.md。

每个重要cell之前都用Markdown解释：

* 代码在做什么；
* 单细胞生物学意义；
* 潜在的数据泄漏风险；
* 正常输出是什么；
* 什么时候应该停止。

====================
二十一、测试
======

实现不需要真实GPU的自动化测试。

至少包括：

1. donor split无重叠；
2. reference/query cell IDs无重叠；
3. query model input不含真实labels；
4. query labels全部为Unknown；
5. HVGs只由reference选择；
6. PCA只在reference fit；
7. metric计算与sklearn一致；
8. confidence threshold为0时coverage为1；
9. threshold提高时coverage不能增加；
10. 缺失class的处理；
11. out-of-reference class的处理；
12. result CSV schema；
13. counts validation；
14. config validation；
15. synthetic smoke pipeline。

运行：

* Python syntax check
* pytest
* notebook static validation
* scripts `--help`
* configuration dry-run
* `verify_outputs.py`

只有全部验证通过，才能在README写completed。

====================
二十二、verify_outputs.py
=====================

验证：

* data audit存在；
* split manifest存在；
* split无donor/cell overlap；
* sealed labels存在；
* reference和query模型存在；
* baseline与scANVI predictions存在；
* predictions不包含训练时不应存在的true labels；
* 每个prediction cell ID唯一；
* query prediction与sealed labels一一对应；
* metrics包含所需列；
* confusion matrix总数正确；
* confidence在合法范围；
* coverage单调不增；
* 正式图来自真实metrics；
* metadata和版本信息完整；
* README状态与真实输出一致；
* smoke和main结果没有混合。

支持：

python scripts/verify_outputs.py --require-complete-main-run

如果缺少真实训练结果，应非零退出并明确列出缺失项。

====================
二十三、provenance
==============

记录：

* dataset loader
* dataset source URL
* dataset fingerprint
* scvi-tools version
* scanpy version
* anndata version
* PyTorch version
* Lightning version
* CUDA version
* GPU型号
* Python版本
* git commit
* seed
* donor_key
* cell_type_key
* counts_layer
* query donor
* reference donors
* HVGs
* model parameters
* epochs actually completed
* early stopping状态
* runtime
* package lockfile
* official tutorial访问日期

每个正式run都生成：

results/run_metadata.json

模型文件和metadata必须能够对应。

====================
二十四、Colab工作流
============

如果本地没有可靠GPU，创建：

notebooks/heart_mapping_colab.ipynb

它必须：

1. 检查GPU；
2. 安装固定版本依赖；
3. clone用户项目；
4. 加载或下载20k heart atlas；
5. 读取已经固定的split；
6. 运行baseline或加载本地baseline；
7. 训练SCVI；
8. 训练SCANVI；
9. mapping query；
10. 保存models、predictions、training history和metadata；
11. 不在训练阶段加载sealed labels；
12. 最后单独运行evaluation；
13. 生成figures；
14. 运行verify_outputs；
15. 打包结果；
16. 提供zip下载；
17. 可选保存到Google Drive。

COLAB_RUNBOOK.md逐步解释：

* 如何打开notebook；
* 如何选择T4；
* 如何检查GPU；
* 每格在做什么；
* 哪些格不应重复运行；
* 如何判断训练正常；
* 如何处理CUDA OOM；
* 如何保存结果；
* 如何下载zip；
* 如何合并回本地仓库；
* 如何重新运行verification；
* 如何push到GitHub。

不得把免费Colab当作自动化后端，也不得尝试绕过Colab交互界面。

====================
二十五、git规则
=========

.gitignore至少排除：

* raw `.h5ad`
* processed `.h5ad`，除非体积极小且明确允许
* model weights
* checkpoints
* caches
* logs中的临时大文件
* notebook checkpoints
* credentials
* Google Drive mount
* environment secrets
* IDE私有状态
* zip archives

应提交：

* code
* configs
* tests
* documentation
* small metrics CSV
* figures
* split manifest
* provenance
* predictions CSV（如果不大且无敏感信息）
* analysis summary

不要提交真实Google token、GitHub token或API key。

没有用户授权时：

* 可以初始化git；
* 可以准备commit；
* 不要猜远程仓库；
* 不要强行push。

====================
二十六、执行顺序
========

Phase 1：

* 环境审计
* 官方API检查
* 项目结构
* 依赖文件
* 数据加载

Phase 2：

* 完整数据审计
* 确认counts、donor和cell-type fields
* 确定query donor
* 固定split
* 实现label sealing
* 运行leakage tests

Phase 3：

* 实现并运行PCA + kNN baseline
* 保存predictions
* 暂不读取真实labels

Phase 4：

如果GPU可用：

* 训练SCVI reference
* 训练SCANVI reference
* 保存模型与latent
* 执行query mapping
* 生成predictions

如果GPU不可用：

* 完成Colab notebook和runbook
* 运行所有CPU测试
* 状态设为READY_FOR_COLAB
* 不生成假结果

Phase 5：

只有所有预测已经冻结后：

* 读取sealed query labels
* evaluation
* confidence–coverage analysis
* figures
* analysis summary

Phase 6：

* 完整pytest
* verify_outputs
* 检查README措辞
* 检查git status
* 检查大型文件与凭据
* 输出最终报告

====================
二十七、最终状态
========

只允许报告以下状态之一：

* STATIC_PIPELINE_READY
* READY_FOR_COLAB
* BASELINE_COMPLETED
* MAIN_RUN_COMPLETED
* MAIN_RUN_VERIFIED

只有完整的真实query mapping和evaluation完成，且：

python scripts/verify_outputs.py --require-complete-main-run

通过后，才能使用：

MAIN_RUN_VERIFIED

smoke test不能算main run。

====================
二十八、完成后向我报告
===========

不要只回复“done”。必须报告：

1. 环境和执行后端。
2. 实际使用的scvi-tools版本。
3. AnnData结构。
4. raw counts实际位置及确认方法。
5. donor_key、batch_key、cell_type_key。
6. query donor选择规则和最终ID。
7. reference/query cell数量。
8. reference/query donor是否完全隔离。
9. query label sealing检查结果。
10. HVG是否只从reference选择。
11. baseline是否完成。
12. SCVI、SCANVI是否完成。
13. query mapping是否完成。
14. 实际训练时间。
15. baseline和scANVI的真实指标。
16. 表现最差的cell types。
17. confidence threshold提高后coverage与accuracy怎样变化。
18. pytest结果。
19. verify_outputs结果。
20. 创建和修改的文件。
21. 仍然存在的限制。
22. 我本人必须做的下一步。

如果需要我手动在Colab运行，请明确指出：

* 打开哪个notebook；
* 运行哪几个cell；
* 正常输出是什么；
* 哪一步结束后把什么文件下载回来。

现在开始。第一步只能进行环境审计、官方API核对和数据审计。在确认raw counts、donor_key、cell_type_key之前，不要开始训练任何模型。
